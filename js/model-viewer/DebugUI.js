import * as dat from 'dat.gui';

// window that is displayed at top right corner of webpage for better debugging (using dat.gui)
// - this is not used in production
const gui = new dat.GUI();

export default gui;